// Types for database
type Id = string;

type Image = Blob;

type Email = string;

type Name = string;

type Password = string;

type authToken = string;

export {Email,Name,Password,Id,Image,authToken};
